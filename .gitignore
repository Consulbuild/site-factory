node_modules/
dist/
# stato Claude Code per-macchina / per-task: mai versionato
.claude/settings.local.json
.claude/scope.json
.astro/
.next/
.DS_Store
*.log
.env
.env.*
site-renderer/out/
# artifact di build per-cliente (build.ts pulisce e ricopia da out/<slug>/img)
site-renderer/public/media/
test-results/
playwright-report/
site-renderer/presets/out/
# screenshot di siti TERZI: uso interno, mai versionati/redistribuiti (TDM:
# "solo il tempo necessario" — si eliminano a preset pubblicato)
factory/references/*/screenshot-*.png
# log verbosi delle run di fabbrica (eventi UI + record curato di debug): NON sono
# provenienza — run.json / gates/*.json / candidate.tokens.json restano tracciati
factory/runs/*/run.ndjson
factory/runs/*/record.ndjson
# scorie di dembrandt (extract-tokens salva già il suo artifact nel riferimento)
site-renderer/output/
